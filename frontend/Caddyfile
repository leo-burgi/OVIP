:{$PORT}
root * /usr/share/caddy

@api path /api/*
respond @api "El frontend recibió una llamada /api. Configurá REACT_APP_BACKEND_URL con la URL pública del backend y redeployá el frontend." 502

try_files {path} /index.html
file_server
